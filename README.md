# MAC Changer (Python)

This project is a simple Python-based MAC address changer for Linux systems.
It uses the `macchanger` utility to spoof the MAC address of a network interface.

⚠️ Changes made using this script are **temporary** and will reset after reboot.

---

## 🚀 Features

- Manual MAC address spoofing
- Random MAC address generation
- Uses system commands via Python (`subprocess`)
- Lightweight and easy to understand
- Beginner-friendly project

---

## 🛠 Requirements

- Linux OS
- Python 3
- macchanger
- Root privileges

Install macchanger:
```bash
sudo apt install macchanger
